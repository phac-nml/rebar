name: Test

on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - '*'
  release:
    types: [published]


env:
  tag: "2023-11-17"
  GITHUB_USERNAME: ${{ github.actor }}
  GITHUB_TOKEN: ${{ github.token }}

jobs:

  build:
    uses: ./.github/workflows/build.yaml

  # ---------------------------------------------------------------------------
  test:

    needs: build
    if: ${{ always() }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64-unknown-linux-musl
            os: ubuntu-latest
          - arch: x86_64-pc-windows-gnu
            os: windows-latest
          - arch: x86_64-apple-darwin
            os: macos-latest

    runs-on: ${{ matrix.os }}

    steps:
      - name: checkout repo
        uses: actions/checkout@v3

      - name: download binary from build
        uses: actions/download-artifact@v3
        with:
          name: rebar-${{ matrix.arch }}
          path: target/${{ matrix.arch }}/release

      - name: install
        if:
        run: |
          chmod +x target/${{ matrix.arch }}/release/rebar
          cp target/${{ matrix.arch }}/release/rebar .
          ./rebar --help

      - name: dataset list
        run: |
          ./rebar dataset list

      - name: dataset download
        run: |
          ./rebar dataset download --name sars-cov-2 --tag $tag --output-dir dataset/sars-cov-2/$tag

      - name: example 1
        run: |
          ./rebar run  --dataset-dir dataset/sars-cov-2/$tag --output-dir output/example1 --populations "AY.4.2*,BA.5.2,XBC.1.6*,XBB.1.5.1,XBL"
          ./rebar plot --dataset-dir dataset/sars-cov-2/$tag --output-dir output/example1

      - name: example 2
        run: |
          ./rebar run  --dataset-dir dataset/sars-cov-2/$tag --output-dir output/example2 --alignment data/example2.fasta
          ./rebar plot --dataset-dir dataset/sars-cov-2/$tag --output-dir output/example2

      - name: toy1
        run: |
          ./rebar run  --dataset-dir dataset/toy1 --output-dir output/toy1 --populations "*" --mask 0,0 --min-length 1
          ./rebar plot --dataset-dir dataset/toy1 --output-dir output/toy1

      - name: upload output
        uses: actions/upload-artifact@v3
        with:
          name: output-${{ matrix.arch }}
          path: output
          retention-days: 1
